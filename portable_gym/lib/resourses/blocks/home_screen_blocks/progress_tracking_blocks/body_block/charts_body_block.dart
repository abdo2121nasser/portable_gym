import 'package:flutter/material.dart';



class ChartsBodyBlock extends StatelessWidget {
  const ChartsBodyBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        LinearProgressIndicator()
      ],
    );
  }
}
